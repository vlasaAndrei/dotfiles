backend = "glx";
vsync = true;

# Shadows for ALL windows
shadow = true;
shadow-radius = 12;
shadow-opacity = 0.75;
shadow-offset-x = -15;
shadow-offset-y = -15;

# No exclusions - shadows on all windows
shadow-exclude = [];
